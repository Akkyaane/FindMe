info:
  name: signInUser
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/users/signIn
  body:
    type: json
    data: |-
      {
        "email": "louloulou.doe@gmail.com",
        "password": "$t#3q15LelurA2aP"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status 200", function() {
          expect(res.status).to.equal(200);
        });

        test("Réponse contient id", function() {
          expect(res.body.id).to.exist;
        });

        test("Réponse contient email", function() {
          expect(res.body.email).to.exist;
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
