info:
  name: deleteUser
  type: http
  seq: 1

http:
  method: DELETE
  url: http://localhost:3000/users/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status 200", function() {
          expect(res.status).to.equal(200);
        });

        test("Message de confirmation", function() {
          expect(res.body.message).to.equal("Utilisateur supprimé avec succès");
        });
